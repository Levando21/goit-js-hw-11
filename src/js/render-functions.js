//const markup = users.map((user) => {
//        return `<li>
//<p><b>Name</b>: ${user.name}</p>
//<p><b>Email</b>: ${user.email}</p>
//<p><b>Company</b>: ${user.company.name}</p>
//</li>`;
//})
//.join("");

export const searchBar = document.getElementById('search');
export const searchBtn = document.getElementById('myButton');
export const allCase = document.querySelector('.search-bar');
